{
  "name": "follow",
  "version": "0.1.0",
  "description": "Follow people, not platforms.",
  "keywords": [
    "follow",
    "oauth",
    "api",
    "twitter",
    "youtube"
  ],
  "homepage": "https://github.com/moderatemisbehaviour/follow/",
  "bugs": {
    "url": "https://github.com/moderatemisbehaviour/follow/issues"
  },
  "license": "UNLICENSED",
  "author": {
    "name": "Daniel Metcalfe",
    "email": "mrdanielmetcalfe@gmail.com",
    "url": "http://danielmetcalfe.rocks/"
  },
  "scripts": {
    "postinstall": "(cd server && npm install) && (cd client && npm install) && (cd client && npm run build)",
    "build-client": "cd client && npm run build",
    "start": "cd server && npm start",
    "start:dev": "concurrently \"cd server && npm run start:dev\" \"cd client && npm start\"",
    "lint": "eslint client/src server/src",
    "test": "npm run cypress:run",
    "test:dev": "npm run cypress:open",
    "jest": "(cd client && npm test) && (cd server && npm test)",
    "ci": "start-server-and-test heroku-like http://localhost:4000",
    "heroku-like": "heroku local",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --config baseUrl=http://localhost:4000"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/moderatemisbehaviour/follow.git"
  },
  "devDependencies": {
    "concurrently": "^3.6.0",
    "cypress": "^3.4.1",
    "eslint": "^5.9.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-cypress": "^2.1.2",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-node": "^8.0.0",
    "eslint-plugin-promise": "^4.0.1",
    "eslint-plugin-react": "^7.11.1",
    "eslint-plugin-standard": "^4.0.0",
    "start-server-and-test": "^1.7.11"
  },
  "dependencies": {
    "dotenv": "^6.2.0"
  }
}
